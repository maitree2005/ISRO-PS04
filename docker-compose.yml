version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    env_file:
      - .env.example
  redis:
    image: redis:7
    ports:
      - "6379:6379"
